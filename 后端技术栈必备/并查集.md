```language
int Find(int x, vector<int>& fa){
	if(fa[x]!=x){
		fa[x] = Find(fa[x],fa);
	}
	return fa[x];
}

int Merge(int x, int y, vector<int>& fa){
	int fx = Find(x,fa);
	int fy = Find(y,fa);
	if(fx==fy){
		return -1;
	}
	else{
		fa[fy] = fx;
		return 1;
	}
}
```
